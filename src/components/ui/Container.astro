---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className = '', narrow = false } = Astro.props;
const maxWidth = narrow ? 'max-w-3xl' : 'max-w-6xl';
---

<div class={`${maxWidth} mx-auto px-6 lg:px-8 ${className}`}>
  <slot />
</div>
